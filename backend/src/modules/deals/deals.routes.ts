import{Router}from"express";import{authenticate}from"../../middleware/auth.middleware.js";import{requireTenant}from"../../middleware/tenant.middleware.js";import{validate}from"../../middleware/validate.middleware.js";import * as c from"./deals.controller.js";import * as s from"./deals.schema.js";export const dealsRouter=Router();dealsRouter.use(authenticate,requireTenant);dealsRouter.get("/pipeline",c.pipeline);dealsRouter.get("/",c.list);dealsRouter.post("/",validate(s.createSchema),c.create);dealsRouter.get("/:id",validate(s.idSchema),c.get);dealsRouter.patch("/:id",validate(s.updateSchema),c.update);dealsRouter.delete("/:id",validate(s.idSchema),c.remove);dealsRouter.post("/:id/move",validate(s.moveSchema),c.move);
