import{z}from"zod";const body=z.object({name:z.string().min(1),amount:z.coerce.number().nonnegative().optional(),currency:z.string().length(3).optional(),stageId:z.string().min(1).max(36),priority:z.enum(["LOW","MEDIUM","HIGH"]).optional(),probability:z.number().int().min(0).max(100).optional(),expectedCloseDate:z.coerce.date().nullable().optional(),contactId:z.string().min(1).max(36).nullable().optional(),accountId:z.string().min(1).max(36).nullable().optional(),ownerUserId:z.string().min(1).max(36).nullable().optional(),description:z.string().nullable().optional(),customFields:z.record(z.unknown()).optional()});const params=z.object({id:z.string().min(1).max(36)});export const createSchema=z.object({body,query:z.any(),params:z.any()});export const updateSchema=z.object({body:body.partial(),query:z.any(),params});export const idSchema=z.object({body:z.any(),query:z.any(),params});export const moveSchema=z.object({body:z.object({stageId:z.string().min(1).max(36),lostReason:z.string().optional()}),query:z.any(),params});
