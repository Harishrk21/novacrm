import {Router} from "express"; import {authenticate,requirePlatform} from "../../middleware/auth.middleware.js"; import {validate} from "../../middleware/validate.middleware.js"; import * as c from "./platform.controller.js"; import * as s from "./platform.schema.js";
export const platformRouter=Router(); platformRouter.use(authenticate,requirePlatform); platformRouter.get("/tenants",c.listTenants); platformRouter.post("/tenants",validate(s.createTenantSchema),c.createTenant); platformRouter.patch("/tenants/:id",validate(s.updateTenantSchema),c.updateTenant); platformRouter.post("/tenants/:id/suspend",validate(s.idSchema),c.suspendTenant); platformRouter.get("/business-categories",c.listCategories); platformRouter.post("/business-categories",validate(s.createCategorySchema),c.createCategory); platformRouter.patch("/business-categories/:id",validate(s.updateCategorySchema),c.updateCategory); platformRouter.get("/tips",c.listTips); platformRouter.get("/dashboard/stats",c.stats);
