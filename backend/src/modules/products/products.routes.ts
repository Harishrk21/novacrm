import{Router}from"express";import{authenticate}from"../../middleware/auth.middleware.js";import{requireTenant}from"../../middleware/tenant.middleware.js";import{validate}from"../../middleware/validate.middleware.js";import * as c from"./products.controller.js";import * as s from"./products.schema.js";export const productsRouter=Router();productsRouter.use(authenticate,requireTenant);productsRouter.get("/",c.list);productsRouter.post("/",validate(s.createSchema),c.create);productsRouter.get("/:id",validate(s.idSchema),c.get);productsRouter.patch("/:id",validate(s.updateSchema),c.update);productsRouter.delete("/:id",validate(s.idSchema),c.remove);
